# Purian Website

Static site for Purian.

## Development
Install dependencies (none).  
Open `index.html` in a browser or serve locally with any static server.

## Deployment
Automatically deployed via Vercel on push to `main`.
